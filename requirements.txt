dash==2.18.2
plotly==5.24.1
pandas==2.2.3
numpy==2.1.3
pyarrow==18.1.0
requests==2.32.3
pydantic==2.10.6
pydantic-settings==2.7.1
python-dotenv==1.0.1
pandera==0.22.1
statsmodels==0.14.4
# scipy must be pinned, not left to the resolver: statsmodels 0.14.4 imports
# scipy._lib._util._lazywhere, which scipy removed in 1.16. Without this pin a
# fresh install picks up the latest scipy and `import statsmodels.api` raises
# ImportError, so the pipeline cannot run at all.
scipy==1.15.3
matplotlib==3.10.0
pytest==8.3.4
ruff==0.8.4
